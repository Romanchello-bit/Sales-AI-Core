{
  "name": "Python 3",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "app.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'set -euo pipefail; python -m venv .venv; source .venv/bin/activate; python -m pip install --upgrade pip; if [ -f requirements.txt ]; then python -m pip install -r requirements.txt; fi; chmod +x scripts/*.sh; echo \"✅ postCreate completed. Activate venv: source .venv/bin/activate\"'",
  "postAttachCommand": "echo 'Devcontainer attached. Use scripts/dev_start.sh or scripts/dev_tests.sh to run the project or tests.'",
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [8501]
}